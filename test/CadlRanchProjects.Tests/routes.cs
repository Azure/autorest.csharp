using System.Threading.Tasks;
using AutoRest.TestServer.Tests.Infrastructure;
using Microsoft.Extensions.Hosting;
using NUnit.Framework;
using Routes;

namespace CadlRanchProjects.Tests
{
    public class RoutesTests : CadlRanchTestBase
    {
        //[Test]
        //public Task Routes_PathParameters_ReservedExpansion_template() => Test(async (host) =>
        //{
        //    await new RoutesClient(host, null).GetPathParametersClient().GetPathParametersReservedExpansionClient().TemplateAsync("foo/bar baz");
        //});

        //[Test]
        //public Task Routes_PathParameters_ReservedExpansion_annotation() => Test(async (host) =>
        //{
        //    await new RoutesClient(host, null).GetPathParametersClient().GetPathParametersReservedExpansionClient().AnnotationAsync("foo/bar baz");
        //});
    }
}
