<?xml version="1.0" encoding="utf-8"?>
<doc>
  <members>
    <member name="CreateJobAsync(WaitUntil,JobData,CancellationToken)">
<example>
This sample shows how to call CreateJobAsync with required parameters.
<code><![CDATA[
var client = new LegacyClient();

var jobData = new JobData("<comment>");
var operation = await client.CreateJobAsync(WaitUntil.Completed, jobData);
]]></code>
</example>
    </member>
    <member name="CreateJob(WaitUntil,JobData,CancellationToken)">
<example>
This sample shows how to call CreateJob with required parameters.
<code><![CDATA[
var client = new LegacyClient();

var jobData = new JobData("<comment>");
var operation = client.CreateJob(WaitUntil.Completed, jobData);
]]></code>
</example>
    </member>
    <member name="CreateJobAsync(WaitUntil,RequestContent,RequestContext)">
<example>
This sample shows how to call CreateJobAsync with required parameters and request content and parse the result.
<code><![CDATA[
var client = new LegacyClient();

var data = new {
    comment = "<comment>",
};

var operation = await client.CreateJobAsync(WaitUntil.Completed, RequestContent.Create(data));

BinaryData responseData = operation.Value;
JsonElement result = JsonDocument.Parse(responseData.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("jobId").ToString());
Console.WriteLine(result.GetProperty("comment").ToString());
Console.WriteLine(result.GetProperty("status").ToString());
Console.WriteLine(result.GetProperty("errors")[0].GetProperty("error").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("errors")[0].GetProperty("error").GetProperty("message").ToString());
Console.WriteLine(result.GetProperty("errors")[0].GetProperty("error").GetProperty("target").ToString());
Console.WriteLine(result.GetProperty("errors")[0].GetProperty("error").GetProperty("innererror").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("results")[0].ToString());
]]></code>
</example>
    </member>
    <member name="CreateJob(WaitUntil,RequestContent,RequestContext)">
<example>
This sample shows how to call CreateJob with required parameters and request content and parse the result.
<code><![CDATA[
var client = new LegacyClient();

var data = new {
    comment = "<comment>",
};

var operation = client.CreateJob(WaitUntil.Completed, RequestContent.Create(data));

BinaryData responseData = operation.Value;
JsonElement result = JsonDocument.Parse(responseData.ToStream()).RootElement;
Console.WriteLine(result.GetProperty("jobId").ToString());
Console.WriteLine(result.GetProperty("comment").ToString());
Console.WriteLine(result.GetProperty("status").ToString());
Console.WriteLine(result.GetProperty("errors")[0].GetProperty("error").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("errors")[0].GetProperty("error").GetProperty("message").ToString());
Console.WriteLine(result.GetProperty("errors")[0].GetProperty("error").GetProperty("target").ToString());
Console.WriteLine(result.GetProperty("errors")[0].GetProperty("error").GetProperty("innererror").GetProperty("code").ToString());
Console.WriteLine(result.GetProperty("results")[0].ToString());
]]></code>
</example>
    </member>
  </members>
</doc>