<?xml version="1.0" encoding="utf-8"?>
<doc>
  <members>
    <member name="DeleteAsync(Int32,RequestContext)">
<example>
This sample shows how to call DeleteAsync with required parameters.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

Response response = await client.DeleteAsync(1234);
Console.WriteLine(response.Status);
]]></code>
</example>
<remarks>
Delete a pet.
</remarks>
    </member>
    <member name="Delete(Int32,RequestContext)">
<example>
This sample shows how to call Delete with required parameters.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

Response response = client.Delete(1234);
Console.WriteLine(response.Status);
]]></code>
</example>
<remarks>
Delete a pet.
</remarks>
    </member>
    <member name="ReadAsync(Int32,RequestContext)">
<example>
This sample shows how to call ReadAsync with required parameters and parse the result.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

Response response = await client.ReadAsync(1234);

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("tag").ToString());
Console.WriteLine(result.GetProperty("age").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>Pet</c>:
<code>{
  name: string, # Required.
  tag: string, # Optional.
  age: number, # Required.
}
</code>

</remarks>
    </member>
    <member name="Read(Int32,RequestContext)">
<example>
This sample shows how to call Read with required parameters and parse the result.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

Response response = client.Read(1234);

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("tag").ToString());
Console.WriteLine(result.GetProperty("age").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>Pet</c>:
<code>{
  name: string, # Required.
  tag: string, # Optional.
  age: number, # Required.
}
</code>

</remarks>
    </member>
    <member name="CreateAsync(RequestContent,RequestContext)">
<example>
This sample shows how to call CreateAsync with required request content, and how to parse the result.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

var data = new {
    name = "<name>",
    age = 1234,
};

Response response = await client.CreateAsync(RequestContent.Create(data));

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("age").ToString());
]]></code>
This sample shows how to call CreateAsync with all request content, and how to parse the result.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

var data = new {
    name = "<name>",
    tag = "<tag>",
    age = 1234,
};

Response response = await client.CreateAsync(RequestContent.Create(data));

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("tag").ToString());
Console.WriteLine(result.GetProperty("age").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the request and response payloads.

Request Body:

Schema for <c>Pet</c>:
<code>{
  name: string, # Required.
  tag: string, # Optional.
  age: number, # Required.
}
</code>

Response Body:

Schema for <c>Pet</c>:
<code>{
  name: string, # Required.
  tag: string, # Optional.
  age: number, # Required.
}
</code>

</remarks>
    </member>
    <member name="Create(RequestContent,RequestContext)">
<example>
This sample shows how to call Create with required request content, and how to parse the result.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

var data = new {
    name = "<name>",
    age = 1234,
};

Response response = client.Create(RequestContent.Create(data));

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("age").ToString());
]]></code>
This sample shows how to call Create with all request content, and how to parse the result.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

var data = new {
    name = "<name>",
    tag = "<tag>",
    age = 1234,
};

Response response = client.Create(RequestContent.Create(data));

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("tag").ToString());
Console.WriteLine(result.GetProperty("age").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the request and response payloads.

Request Body:

Schema for <c>Pet</c>:
<code>{
  name: string, # Required.
  tag: string, # Optional.
  age: number, # Required.
}
</code>

Response Body:

Schema for <c>Pet</c>:
<code>{
  name: string, # Required.
  tag: string, # Optional.
  age: number, # Required.
}
</code>

</remarks>
    </member>
    <member name="GetListPetToysResponsesAsync(String,String,RequestContext)">
<example>
This sample shows how to call GetListPetToysResponsesAsync with required parameters and parse the result.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

Response response = await client.GetListPetToysResponsesAsync("<petId>", "<nameFilter>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("items")[0].GetProperty("id").ToString());
Console.WriteLine(result.GetProperty("items")[0].GetProperty("petId").ToString());
Console.WriteLine(result.GetProperty("items")[0].GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("nextLink").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>ToyListResults</c>:
<code>{
  items: [
    {
      id: number, # Required.
      petId: number, # Required.
      name: string, # Required.
    }
  ], # Required.
  nextLink: string, # Optional.
}
</code>

</remarks>
    </member>
    <member name="GetListPetToysResponses(String,String,RequestContext)">
<example>
This sample shows how to call GetListPetToysResponses with required parameters and parse the result.
<code><![CDATA[
var endpoint = new Uri("<https://my-service.azure.com>");
var client = new PetStoreClient(endpoint);

Response response = client.GetListPetToysResponses("<petId>", "<nameFilter>");

JsonElement result = JsonDocument.Parse(response.ContentStream).RootElement;
Console.WriteLine(result.GetProperty("items")[0].GetProperty("id").ToString());
Console.WriteLine(result.GetProperty("items")[0].GetProperty("petId").ToString());
Console.WriteLine(result.GetProperty("items")[0].GetProperty("name").ToString());
Console.WriteLine(result.GetProperty("nextLink").ToString());
]]></code>
</example>
<remarks>
Below is the JSON schema for the response payload.

Response Body:

Schema for <c>ToyListResults</c>:
<code>{
  items: [
    {
      id: number, # Required.
      petId: number, # Required.
      name: string, # Required.
    }
  ], # Required.
  nextLink: string, # Optional.
}
</code>

</remarks>
    </member>
  </members>
</doc>