@using System.Linq
@inherits TemplateCs<AutoRest.CSharp.Model.CompositeTypeCs>
@Header("// ")
@EmptyLine
namespace @(Settings.Namespace).@(Settings.ModelsName)
{
@EmptyLine
    @XmlDocSummary($"Exception thrown for an invalid response with {Model.Name} information.")
    public partial class @Model.ExceptionTypeDefinitionName : Microsoft.Rest.RestException
    {
        @XmlDocProp(false, "information about the associated HTTP request")
        public Microsoft.Rest.HttpRequestMessageWrapper Request { get; set; }
@EmptyLine
        @XmlDocProp(false, "information about the associated HTTP response")
        public Microsoft.Rest.HttpResponseMessageWrapper Response { get; set; }
@EmptyLine
        @XmlDocProp(true, "the body object")
        public @Model.Name Body { get; set; }
@EmptyLine
        @XmlDocCtor(Model.ExceptionTypeDefinitionName)
        public @(@Model.ExceptionTypeDefinitionName)()
        {
        }
@EmptyLine
        @XmlDocCtor(Model.ExceptionTypeDefinitionName)
        @XmlDocParam("message", "exception message")
        public @(Model.ExceptionTypeDefinitionName)(string message)
            : this(message, null)
        {
        }
@EmptyLine
        @XmlDocCtor(Model.ExceptionTypeDefinitionName)
        @XmlDocParam("message", "exception message")
        @XmlDocParam("innerException", "inner exception")
        public @(Model.ExceptionTypeDefinitionName)(string message, System.Exception innerException)
            : base(message, innerException)
        {
        }
    }
}